import 'package:flutter/material.dart';
import '../../resources/map2.dart';

class MapSearch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: MapSample(),
    );
  }
}